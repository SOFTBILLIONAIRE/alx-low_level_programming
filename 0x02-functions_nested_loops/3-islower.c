#include "main.h"
/**
 * _islower - Entry point
 *
 * Return: Allow success
*/
int main
{
	int r = _islower(b);
	
	if (b >= 'a' && b <= 'z')
	{
		return(1);
	}
	else
	{
		return (0);
	}
	return (0);
}
