#include "main.h"
#include <stdio.h>
/**
 * _isdigit - A function that checks for a digit
 * @c: defines the value of the integer
 * Return: Allow success
*/
int _isdigit(int c)
{
	if (c >= '0' && c <= '9')
	{
		return (1);
	}
	else
	{
		return (0);
	}
return (0);
}
