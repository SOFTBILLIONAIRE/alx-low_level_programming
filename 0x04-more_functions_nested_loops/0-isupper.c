#include "main.h"
#include <stdio.h>
/**
 * _isupper - is a function that checks for uppercase
 * @c: describes the parameter for the function
 * Return: Allow success
*/
int _isupper(int c)
{
	if (c >= 66 && c <= 90)
	{
		return (1);
	}
	else
	{
		return (0);
	}
return (0);
}
