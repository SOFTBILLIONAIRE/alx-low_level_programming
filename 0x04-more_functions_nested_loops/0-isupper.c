#include "main.h"
#include <stdio.h>
/**
 * main - _isupper is a function that checks for uppercase
 *
 * Return: Allow success
*/
int _isupper(int c)
{
	int n;
	
	if (n >= 66 && n <= 90)
	{
		return (1);
	}
	else
	{
		return (0);
	}
return (0);
}
