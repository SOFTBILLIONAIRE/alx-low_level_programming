#include "main.h"
/**
 * positive_or_negative - A function that prints if integer is negative or pos.
 *
 * Return: Allow success
*/
void positive_or_negative(int i)
{
	        int i;

		        i = 0;
			        positive_or_negative(i);

				        return (0);
}
