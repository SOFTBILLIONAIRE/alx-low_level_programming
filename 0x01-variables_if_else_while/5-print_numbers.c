#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - Entry point
 *
 * Return: Always success
*/
int main(void)
{
	int b;

	b = 0;
	while (b <= 9)
	{
		printf("%d", b);
		b++;
	}
	{
		printf("\n");
	}
	return (0);
}
